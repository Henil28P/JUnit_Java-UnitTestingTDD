junit.jupiter.execution.parallel.enabled = true
junit.jupiter.execution.parallel.config.strategy = dynamic
# this will change the properties for all our tests.
# However, they don't run parallel automatically - in order to achieve this, we have to indicate on top of the class by adding @Execution(<mode>)